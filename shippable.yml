language: none

sudo: required

runtime:
  nodePool: shippable_shared_aarch64
 
build:
 ci:
  - apt update && apt upgrade && apt install git build-essential
  - mkdir cmake-build && cd cmake-build && ../cmake-source/bootstrap && make -j$(nproc)
